<!DOCTYPE AFC>
<algorithm version="1.2">
  <branch>
    <post cond="d0 > ε">
      <branch>
        <process text="d0 = pow(1. / 2, n) + pow(1. / 3, n)"/>
        <process text="d += d0"/>
        <process text="n++"/>
      </branch>
    </post>
    <ou vars="d"/>
  </branch>
</algorithm>
